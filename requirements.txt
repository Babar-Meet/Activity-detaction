# Activity Detection — Python Dependencies
# ==========================================

# Core computer vision
opencv-python>=4.8.0

# YOLO object detection
ultralytics>=8.0.0

# Pose estimation
mediapipe>=0.10.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# GPU support (PyTorch with CUDA)
# Note: torch is installed separately in setup.bat for CUDA support
